# @package model
name: fmlp_med_genept
type: docker
description: "fMLP (med) with Universal Cell Embeddings"

docker:
  image: "cellsimbench/fmlp:latest"
  memory: "max"
  cpus: "max"
  gpu: true

# Model hyperparameters
hyperparameters:
  # Foundation model configuration
  foundation_model: "genept"
  embedding_key: "embeddings_genept"
  
  # Architecture parameters
  latent_dim: 64
  hidden_dim: 256
  covariate_embedding_dim: 16
  
  # Training parameters
  training_epochs: 100
  batch_size: 256
  learning_rate: 0.001
  val_every: 10
  
  # DEG weighting
  use_weights: false
  
  # Random seed
  seed: 42
